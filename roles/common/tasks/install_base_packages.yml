- name: Install Base Packages
  kewlfft.aur.aur:
    use: "{{ aur_helper }}"
    name:
      - pipewire-jack
      - pipewire-media-session
      - btop
      - neovim
      - chromium
      - firefox
      - neofetch
      - man-db
    state: present
  become: yes
  become_user: "{{ ansible_user }}"